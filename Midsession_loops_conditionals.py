# -----------------------------------------------------
# 1. 
# Write a function that takes an integer and prints 
# that number of rows and columns of asterisks.
# -----------------------------------------------------

# PSEUDOCODE/notes:

# ~ for loop, because its a defined number of iterations.

#- - - - - - - - - - - - - - - - - - - - - - - - - - - - 
# CODE

size = 4

for i in range(size):
    print ('*' * size)



#def draw_asterisk():
   # for i in range(size):
    #	print ('*' * size)
    
    #draw_asterisk()
	
# -------------------------------------------------------
# 2.
# Write a function that draws a checkerboard with x's
# and o's. A checkerboard is an 8 x 8 alternating grid.
# -------------------------------------------------------


# PSEUDOCODE/notes:

# define the size of the checkerboard.
# print alternating x's and o's.

# ~ Use for loop ( for x = 8, for o = 8)
# ~ Use a counter, increment counter
# ~ Use modulo  (%) calc (? which) to determine if odd or even. 
#			(from class notes;
#				x % 2 == 0 -- Even
#				x % 2 == 1 -- Odd
# ~Have a conditional (if) statement to print.
# 		~ print 'X'for even squares
#		~ contional else to print 'O' for odd square

#- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
#CODE:



counter = 0

for x = 8
for y = 8

def draw_checkerboard():

for i in range(size):
    #print ('x' * size)
if
else
print ('o' * size)

# not yet complete


draw_checkerboard():
	